# Changelog

## Unreleased

- Limited the release manifest to protected implementation, proof, evidence,
  schema, vector, SDK, lock, and replay inputs. Pull Requests use the protected
  input mode, while main and candidate-release runs additionally verify the
  protected source-tree root. Documentation and workflow metadata remain
  blocking public-surface inputs without forcing unrelated manifest churn.
- Named the active maintainer and fixed the public tag families to
  `vX.Y.Z-candidate.N` and `vX.Y.Z`.
- Superseded the publicly unreachable formal-foundation commit with the
  reachable public revision through a new versioned lock. The immutable
  historical lock, every ERC proof source and statement, all qualification
  receipts, and all expected hashes remain unchanged. A fourteen-file mapping
  verifies ten byte-exact theories, one byte-exact bibliography input, the
  exact two-line regulatory-action theory relocation, and the session and
  non-proof document split. Hostile validation rejects all fourteen mapped-file
  mutations plus missing-registration, wrong-import, and wrong-commit overlay
  mutations. The temporary sibling-session overlay clean-builds `ERC_TRUST`
  with proof audit PASS and zero oracle dependencies.
- Curated the public release candidate through versioned evidence
  supersession and lossless archive separation. The candidate retains 808
  tracked files; 613 raw, intermediate, obsolete, or development-only files
  remain recoverable from the immutable private archive history. Public
  surface and repository health checks are blocking, generated paths are
  marked for GitHub language statistics, and the public citation identity is
  `Jinwook Kim` with ORCID `0009-0004-4993-8005`.
- Implemented the Jay-approved two-layer runtime-binding qualification without
  changing any historical expected hash. Pinned-solc replay and all six
  semantic projections pass for seven subjects; hostile validation kills
  `42/42` semantic mutations, classifies `7/7` packaging-only mutations as
  drift-PASS, and rejects `7/7` expected-hash overwrites. `STATE-02`,
  `STATE-03`, and `ART-01` now consume the new receipt, completing Supporting
  at `24/24` while Core remains `49/49` and C0-C6 remain byte-unchanged `7/7`.
- Qualified the remaining 35 Core rows through two ROOT-registered Isabelle
  conditional-certificate theories and a fail-closed external binder over the
  unchanged C0-C6 receipts, product-test blocks, and distinguishing negative
  evidence. Core Refinement is `49/49`; Supporting remains `21/24` with three
  runtime-binding identity rows frozen. Added an unapproved two-layer
  runtime-binding qualification proposal without changing the verifier or TCB.
- Qualified 21 of 24 mandatory Supporting rows under the current profile by
  binding existing retrieve/layout/ABI/artifact/nonclaim theorems or checked
  properties to the unchanged terminal package aggregate. Every row has a
  fail-closed distinguishing negative. `STATE-02`, `STATE-03`, and `ART-01`
  remain frozen behind a shared Foundry artifact packaging-versus-payload
  identity gap; the proposed two-layer qualification package was not created
  because it changes a verifier/TCB boundary. No parent package proof, actual
  product counterexample, or optional backlog row was added.
- Qualified current-profile Core row `STATE-05` and an eleven-row
  action/reversal batch by consuming the unchanged C-series terminal
  receipts. Each row retains its existing named refinement theorem, a current
  product inhabitant, and either an executable package mutation or a
  row-local observer mutation. Core Refinement advances to `14/49`; parent
  package proof executions and actual product counterexamples remain zero.
- Qualified current-profile Core row `STATE-04` without rerunning C0-C6. A
  new Isabelle theorem proves that FREEZE and RESTRICT preserve separate
  overlay observations and a same-state conflation negative remains
  distinguishable. The current product supplies a concrete coexistence test,
  the row index binds the unchanged C-series and C4 terminal receipts, Core
  Refinement advances to `2/49`, and actual product counterexamples remain
  zero.
- Terminal-qualified all seven reusable Native Full packages C0-C6. Added
  advanced-sanity Certora proofs and executable mutants for C1 authorization
  and replay, C3 exact-use routing, C4 forward effects, C5 reversal LIFO, and
  C6 transaction commit/rollback receipts; preserved the already-qualified C0
  and C2 runtime binders. The aggregate current-profile replay reports `7/7`,
  ACT-01 `freeze_success_refines` is qualified as Core Refinement `1/49`, and
  central completion credit remains zero.
- Compiled the C0 canonical-decode priority(1) verification-only definition
  and passed an exact-pinned dry-run parse of the PC0-to-PC3477 claim in 262
  seconds without starting a proof backend. The fixed input then reached
  PC3477 under strict no-Booster Kore in 812 seconds with three nodes, one
  edge, 675 rewrites, and no open or unsound graph state. A trace/runtime
  audit reclassified PC3477 as the selector dispatch/function entry before
  CALLVALUE, CALLDATASIZE, and narrow-word checks, so this is not yet the C0-03
  canonical-decode inhabitant. The post-decode positive, dirty-enum negative,
  universal/bridge/build/binder gates, C0, ACT-01, and central credit remain
  open/zero.
- Qualified reusable package C2 by binding the Certora universal classifier
  and killed mutation to strict dependency, actual token STATICCALL,
  returndata-copy, actual-evidence classifier, and wrong-binding typed-revert
  receipts. The separate qualification overlay marks only C2 qualified;
  ACT-01 remains OPEN and central credit remains zero.
- Proved the exact 751-byte dependency runtime's standalone mode-0 approval
  summary under strict no-Booster Kore in 63 seconds (3 nodes, one
  951-rewrite edge, no open or unsound graph state). The theorem is bound to
  the actual 228-byte call input and 128-byte response. TrustToken call-depth
  lifting, consumer/binder closure, C2 runtime qualification, and ACT-01
  credit remain open.
- Rejected sliced KCFG resume after K reparse changed the internal claim
  digest and reinitialized the graph. Added a verification-only PC21900
  identity observer that modifies only the K control cell, plus a fail-closed
  OPEN binder. The cutpoint proof is not yet executed and grants no credit.
- Closed the verification-only C2 classifier in Certora with advanced sanity,
  non-vacuity witnesses, and a killed wrong-binding-echo mutation after the
  official unresolved harness produced no-credit internal UNKNOWN results.
  The actual Native `STATICCALL` and 128-byte returndata boundary are traced
  and the three split K packages parse under the pinned definition. Strict
  Kore remains open: opcode-level CFG storage reached dependency PC344 in 70
  nodes before the 902-second timeout. The saved KCFG is diagnostic only,
  ACT-01 remains OPEN, Core-Refinement remains 0/49, and Legacy remains 6/79.
- Added a versioned current-profile bridge, six hybrid TCB profiles, the
  abstraction-alpha/ActionSummary/ReversalSummary contracts, and the C0 through
  C6 reusable-summary registry without modifying the legacy lock or legacy
  bridge bytes. C0, C2, reusable-summary, and conditional ACT-01 Isabelle
  theories build against the exact pinned foundation snapshot, and the
  unchanged Foundry baseline passes. ACT-01 remains open with zero current
  credit because Certora execution requires explicit approval to transmit the
  disclosed private source/spec payload to the remote Prover; no KEVM heavy
  package was started.
- Reframed the mandatory current profile around the pinned Native Full financial-accident core while
  preserving the complete 79-row assurance inventory. The machine-checked
  scope now separates 49 Core-Refinement rows, 24 mandatory Core-Supporting
  gates, and six nonblocking optional Profile/foundation backlog rows. The
  reusable-first hybrid process assigns compiled-bytecode financial rules to
  Certora, counterexample and mutation preflight to Foundry/Halmos, and exact
  ABI/call/revert/rollback/log/receipt/composition seams to KEVM. ACT-01 is a
  C0/C1/C2/C3/C4/C6 Native FREEZE corollary; continued manual basic-block
  expansion is stopped. This process change adds no row credit.
- Added response-independent RPC lifecycle instrumentation and an ACT-01
  trace/basic-block segmentation canary pipeline. Two adjacent positive
  concrete validation segments (`PC 13431 -> 15224 -> 13465`) now pass as
  independent proofs in one parser batch, with 26/26 explicitly owned cells
  equal at the shared boundary. The result is topology-only: ACT-01 remains
  open, the registry remains 6/79, and universalization, same-entry negatives,
  explicit composition, a row corollary, a separate TCB profile, and
  independent replay remain mandatory before credit.
- Preserved method-v14 as a proof-free diagnostic checkpoint after the full
  deterministic product pipeline passed with 220 nodes, 1,252 edges, zero
  anomalies, two byte-identical fresh roots, and two fail-closed mutations.
  The subsequent 162-replay wave plan also passed without executing the
  authoritative wave.
- Kept ABI-04 open after the no-credit v14 canary for replay 009 failed closed.
  The proof reached the zero-domain guard before reading the malformed `0x106`
  action-enum word, so the same all-zero-envelope contamination risk applies to
  all 45 `high_bits` cases. Method-v14 and both canary roots are diagnostic
  evidence only; no aggregate, registry, ledger, or central credit was added,
  and the historical registry remains 6/79 discharged with 73 open.
- Advanced the strict end-to-end refinement registry to 6/79 discharged and
  73 open by closing ART-03, ART-04, and ART-05. Each row now binds an exact-runtime
  positive KEVM claim, an executable semantic-mutant counterexample, a
  generated and reverse-checked Isabelle/K bridge, an oracle-free named
  Isabelle theorem, and a repository-owned fresh replay; this is an interim
  checkpoint, not a campaign-completion claim.
- Bound ART-05's 14-theory source/import closure to the exact governor runtime
  observation and a one-byte executable semantic mutant. The portable row
  sanitizer excludes local proof-attempt scaffolding, canonical curation
  records the closed positive and terminal negative graphs, and the common
  curator removes only an exhausted pre-replay prerequisite from final PASS
  reports.
- Added the ABI-04 fail-closed transitive hash DAG and deterministic product-v2
  coordinator. Independent JavaScript and Python verifiers now reject missing,
  unexpected, duplicate, or declared/actual-mismatched nodes and invalidate all
  reachable descendants; managed outputs are generated twice, compared
  byte-for-byte, and subjected to clean generator and reverse checks.
- Bound the dynamic-offset S1 coordinator to the exact six-pair, twelve-replay
  canonical-positive and unchanged-claim-mutant-negative graph set, with a
  maximum of two concurrent heavy proofs, pre/post freeze verification, six
  pair binders, and independent post-wave replay. The fresh closure freeze
  passed, but no authoritative proof ran and ABI-04 remains open with no new
  S1 or central credit.
- Invalidated obsolete parse-only receipts after dynamic-offset or
  dynamic-length source regeneration, tightened the corresponding generators
  and reverse checks, corrected the K guard-premise check, and canonicalized
  generation-edge hashing across the JavaScript and Python receipt verifiers.
- Corrected the ABI-04 methodology after an adversarial process audit. A
  generator-owned closure manifest now freezes the complete node exact set and
  real parent/child hashes instead of synthesizing policy-edge declared hashes
  from current values. The gate rejects unexpected allowed-extension files,
  runs full static materialization twice, rebuilds generator-owned outputs in
  two fresh isolated roots, and executes two expected-nonzero fail-closed
  mutation regressions. A proof-free pinned POSIX toolchain preflight now
  validates all 12 S1 commands before heavy KEVM can start. These changes add
  no proof or central credit; ABI-04 and the 162-replay contract remain OPEN.

- Added verification-only policy-binding classifier inputs, a fail-closed CVL
  partition, a binding-echo negative mutation, and Foundry unit coverage while
  keeping low-level call and full-transaction refinement outside the claim.
- Added a repository-level verification architecture diagram that separates
  the Isabelle abstract model, Solidity and Certora checks, compiled EVM
  bytecode, Kontrol and KEVM proofs, the unclaimed full-refinement boundary,
  and the separate deployment boundary.
- Replaced the four architecture Mermaid blocks with tracked SVG diagrams that
  follow the banner palette. GitHub renders a Mermaid block inside an iframe
  whose `src` carries a URL fragment, so browsers scrolled that iframe into
  view on load and opened the page away from the top. The Mermaid definitions
  are kept beside the SVGs as the diagram sources, and the SVGs are now
  required public-surface files.
- Separated licensing by artifact: the proposed ERC text is dedicated under
  CC0 1.0 Universal, while the implementation, SDK, tooling, project assets,
  and other repository content remain under BSD 3-Clause.
- Replaced the literal shield, balance, and receipt-seal emblem with a
  reusable abstract ERC-TRUST mark: mirrored typed state channels cross a
  calibrated protocol boundary and converge on a recomputable witness point
  without implying official ERC endorsement.
- Removed an internal lifecycle label from the public banner so the
  repository identity remains stable across review stages.
- Excluded Certora `.spec` inputs from GitHub's language summary and mapped
  Kontrol/KEVM `.k` assertions to the K Framework classification; the tooling
  map retains their precise roles.
- Added a repository-native tooling and language map, replaced the redundant
  red security-status badge with a conventional informational-blue
  security-policy link, and restyled the landing-page review notice as a
  subdued blockquote without weakening its unaudited or not-for-production
  boundary.
- Removed the rounded tile behind the project mark. A restrained circular
  field and keyline now separate the protocol aperture from the banner
  background without turning the mark into a boxed application icon.
- Reflowed the landing-page and ERC-3643 architecture diagrams into compact
  vertical stage bands for readable GitHub rendering, and added scoped links
  to the RCP and CDSP papers and the related public Isabelle/HOL artifact
  repository.
- Reworked the repository landing page, architecture, integration, profile,
  SDK, security, contribution, governance, support, disclaimer, citation, and
  community-review surfaces for public technical review.
- Added repository-health automation, documentation issue routing, dependency
  updates, feature-branch CI, bounded workflow permissions, concurrency, and
  job timeouts.
- Installed the exact pnpm version before enabling setup-node's pnpm cache, so
  clean GitHub runners can resolve the cache dependency manager.
- Removed the internal forum-post draft from the distributable package. The
  repository now contains a neutral community review guide and no
  pre-authorized external posting instruction.
- Added the immutable six-action native reference, separate reversals,
  versioned fail-closed dependencies, custody/settlement/entitlement records,
  and canonical receipts.
- Added complete ERC-20/165/7943 fungible behavior with same-transaction
  exact-use tickets and raw sensitive-selector closure.
- Added the clean-room ERC-3643 Verified profile, one-way
  `ProfileGovernor`, runtime-codehash topology binding, and direct/batch bypass
  negatives.
- Added unit, fuzz, stateful invariant, bounded Certora, and Kontrol/KEVM
  verification sources plus an eleven-fault mutation campaign.
- Added the pre-ERC draft, community-review guide, integration guide,
  profile documentation, TypeScript operator SDK, canonical receipt schema,
  conformance vectors, public claim matrix, and release manifest tooling.
- Added security/conduct policies, issue and pull-request templates, and
  pinned CI/release-candidate validation workflows.
- Kept the Isabelle theories, ROOT files, release PDF, and abstract-model release manifest
  byte-for-byte unchanged.
- Added the source-bound Isabelle/Solidity applicability assessment. The
  official AFP session clean-builds, but the framework is marked NOT
  APPLICABLE to the security-critical reference kernel; no Solidity-in-Isabelle or
  Isabelle-to-EVM refinement claim is made.

- Colocated the product-owned ERC-TRUST Isabelle session with the Solidity
  implementation under `formal/isabelle/ERC_TRUST/`.
- Added the repository-level `FORMAL_VERIFICATION.md` refinement and
  claim-boundary entry point.
- Kept the abstract-model claim matrix and replay scripts in Git while moving 243
  generated raw-run files to a sealed private archive. The archive hash is
  pinned in `formal-dependencies.lock.json`.
- Preserved the failing Certora run as diagnostic provenance and added the
  corrected 13/13 successful run. The bounded claim is
  `pilot conjunct VERIFIED`.
- Integrated the formal-layout and pilot evidence histories while retaining
  both input commits as parents of the integration history.
- Replaced machine-local replay-script defaults with explicit runtime
  parameters. This portability change reopens only the model source-hash binding;
  the Isabelle model, theories, release PDF, and release manifest are
  unchanged.
- The reference-candidate release freezes the implementation trust boundary. It does not establish
  full six-action refinement, production readiness, audit completion, or
  deployment assurance.
